version: "3.0"

services:

  octonag:
    network_mode: bridge
    build:
      context: ..
      dockerfile: devstack/Dockerfile
    command: python src/pr_reminders.py
    environment:
      - GITHUB_API_TOKEN
      - SLACK_API_TOKEN
      - JIRA_PASS
      - JIRA_USER
