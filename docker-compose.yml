version: "3.0"

services:

  octonag:
    build:
      context: .
      dockerfile: Dockerfile
    command: ./startup.sh
    volumes:
      - .:/src
    environment:
      - GITHUB_API_TOKEN
      - SLACK_API_TOKEN
